# Claude Code 작업 규칙

## 논문 리뷰 / 기사 요약 → Notion 저장

- 논문 정리, 리뷰, 요약 요청이 오면 **텍스트로 답변하지 말고** 반드시 `mcp__notion_finance_digest__save_summary_to_notion` 도구로 Notion에 저장한다.
- 명시적으로 "텍스트로 알려줘"라고 한 경우만 예외.

## Notion 콘텐츠 포맷

- `##`, `###` 같은 마크다운 헤더 사용 금지.
- 섹션 구분은 `━━━ 섹션명 ━━━` 형식 사용.
- 짧은 bullet point 나열이 아닌 문단 형식으로 충분히 서술 (배경, 가설, 실험, 결과, 의의, 한계, 질문 등 포함).
- 이모지 적극 사용 — 섹션 구분선, 핵심 키워드, 수치 강조 등에 자연스럽게 포함한다.

## 다이어그램

- 논문 구조나 개념을 시각적으로 표현하면 이해에 도움이 될 때, ASCII/유니코드 텍스트 다이어그램을 리뷰 본문에 포함한다.
- 단순한 내용이거나 다이어그램이 필요 없는 논문이면 생략해도 된다.
- 다이어그램은 별도 섹션으로 만들지 않고 관련 설명 섹션 안에 자연스럽게 포함한다.

## 개념/용어 설명

- 논문 이해에 필요한 배경 개념이나 용어 설명은 별도 페이지로 만들지 않고, 리뷰 본문 페이지에 섹션으로 통합한다.
- 추후 추가 요청이 오면 기존 페이지를 업데이트한다.

## Notion DB 라우팅

- `category="paper"` → AI 논문 DB (`34617b872be68060a474e18a73510f38`)
- `category="stock_research"` → 주식 리서치 DB (`39bf50a1aca04ad2a10079e958cf96d4`) — 뉴스 / 산업분석 / 주간브리핑 / 포트폴리오
- `category="stock_study"` → 주식 공부노트 DB (`d1afcb876857487eb978c1a8e0952d05`) — 용어 / 이론 / 지표 / 전략 / 산업지식

모두 "Finance & 투자" 페이지(`33117b872be68187a1b4ddc51261856e`) 인라인 DB.

## 주식 리서치 저장 규칙

- `sub_category`: 뉴스 | 산업분석 | 주간브리핑 | 포트폴리오
- `source`: 출처 (예: "한국경제", "연합뉴스")
- `tags`: 관련 섹터/종목 (예: ["반도체", "AI", "국내"])
- `icon`: 페이지 아이콘 이모지 (예: "📰", "📊", "🔍", "💼")
- `difficulty`: stock_study 전용 — 기초 | 중급 | 고급

## 주식 콘텐츠 포맷

- 논문과 동일하게 `##`, `###` 헤더 금지. `━━━ 섹션명 ━━━` 형식 사용.
- 뉴스/리포트: 핵심 요약 → 시장 영향 → 투자 시사점 순으로 서술.
- 공부노트: 개념 정의 → 실제 사례 → 투자 적용법 순으로 서술.
- 단순 bullet 나열 금지, 문단 형식으로 충분히 서술.
- 이모지 적극 사용 — 섹션 구분선, 수치, 추천/주의 표시 등에 자연스럽게 포함한다.

## 일일 자동화 (daily_action.py / daily.py)

- `daily_action.py` — GitHub Actions 전용 (운영 배치)
- `daily.py` — 로컬 Windows 실행용

로컬 실행 명령:

```powershell
& "D:\Anaconda3\envs\project\python.exe" "D:\projects\python\notion-finance-digest\daily.py"
```

화·금만 실행. 한국 공휴일 자동 스킵. 오늘 날짜 페이지가 이미 있으면 항목별 스킵.

### 자동 저장 항목 4개

| 항목 | 제목 | 아이콘 | sub_category |
|------|------|--------|--------------|
| ① 뉴스 종합 | `[날짜] 경제 뉴스 종합` | 📰 | 뉴스 |
| ② 주간브리핑 | `[날짜] 주간 투자 브리핑` | 📊 | 주간브리핑 |
| ③ Claude 인사이트 | `[날짜] Claude 인사이트` | 🔍 | 산업분석 |
| ④ 포트폴리오 브리핑 | `[날짜] 포트폴리오 브리핑` | 💼 | 포트폴리오 |

### 실행 순서

1. **오래된 항목 자동 삭제** — `RETENTION_DAYS` 기준 초과 항목 아카이브
2. **중복 체크** — 오늘 날짜 페이지 이미 존재하면 해당 항목 스킵
3. **데이터 수집** — RSS·yfinance·네이버 금융 병렬 수집
4. **Claude 요약 생성** — 필요한 항목만 병렬 API 호출
5. **Notion 저장** — stock_research DB에 페이지 생성

### 오래된 항목 자동 삭제 (`RETENTION_DAYS`)

```python
RETENTION_DAYS = {
    "뉴스":       14,   # 2주
    "주간브리핑": 30,   # 1달
    "산업분석":   60,   # 2달
    "포트폴리오": 60,   # 2달
}
```

매 실행 시 신규 저장 전에 자동 삭제. 값은 `daily_action.py` 상단에서 조정.

### ③ Claude 인사이트 섹션 구성 (5개)

1. 주목 산업 심층 분석
2. 주요 지수 전망
3. 시장이 놓치고 있는 것
4. **ETF 추천** — 한국 ETF(TIGER·KODEX·SOL·ACE 등) + 해외 ETF(QQQ·SPY 등) 각 1~2개, 진입 근거·리스크 포함
5. Claude의 핵심 의견

### ④ 포트폴리오 브리핑 데이터 소스 및 섹션

보유 종목 정보는 `PORTFOLIO_HOLDINGS` 상수에 하드코딩 (포트폴리오 변경 시 업데이트 필요).

섹션 구성 (4개):
1. 보유 종목별 현황 분석
2. 이번 주 매수 타이밍 판단
3. 포트폴리오 리스크 점검
4. Claude의 포트폴리오 인사이트

### 데이터 소스 (③④ 공통)

- 연합뉴스 RSS (국내 경제)
- 한국경제·매일경제 RSS (국내 경제지)
- Reuters·CNBC·MarketWatch·연합뉴스 국제 RSS (해외)
- 네이버 금융 리서치 (증권사 리포트 제목·날짜) — ③만 사용
- yfinance 실시간 시장 데이터 (KOSPI·KOSDAQ·S&P500·NASDAQ·달러인덱스·WTI·금·달러/원)

## 주간 브리핑 프로토콜

"주간 브리핑", "이번 주 투자 추천", "시장 브리핑" 등의 요청이 오면 아래 순서로 수집·분석 후 Notion에 저장한다.

수집 항목:
1. 글로벌 거시경제 — 미국 금리(연준 동향), 달러인덱스, 유가(WTI), 금 가격
2. 미국 시장 — S&P500 / 나스닥 주간 등락, 주요 ETF(QQQ·SPY·SOXX 등), 핵심 뉴스
3. 국내 시장 — KOSPI / KOSDAQ 주간 동향, 외국인·기관 수급, 주요 이슈
4. 유망 섹터 분석 — 현재 모멘텀 상위 섹터 2~3개, 이유 설명
5. ETF/종목 추천 — 섹터별 대표 ETF 또는 종목, 투자 근거 및 리스크

저장 방법:
- `category="stock_research"`, `sub_category="주간브리핑"`
- `title`: "YYYY-MM-DD 주간 투자 브리핑"
- `tags`: 해당 주 핵심 섹터 태그 포함
- `source`: "Claude 자동 수집"
